---
title: "Z-Image Turbo"
description: "超快速图像生成，亚秒级推理延迟"
api: "POST /v1/images/generations"
---

## 概述

Z-Image-Turbo 是 Z-Image 的蒸馏加速版本，仅需 8 次函数评估（NFE）即可达到或超越领先竞品的效果。在企业级 H800 GPU 上实现亚秒级推理延迟，同时也能在 16GB 显存的消费级设备上流畅运行。

## 主要特点

<CardGroup cols={2}>
  <Card title="极速生成" icon="bolt">
    亚秒级推理延迟，实时创作工作流
  </Card>
  <Card title="高质量输出" icon="sparkles">
    保持细节、光影和构图的高保真度
  </Card>
  <Card title="低资源占用" icon="microchip">
    16GB 显存即可运行，适合消费级设备
  </Card>
  <Card title="双语支持" icon="language">
    支持中英文提示词和文字渲染
  </Card>
</CardGroup>

## 请求参数

<ParamField body="model" type="string" required>
  模型名称，固定为 `z-image-turbo`
</ParamField>

<ParamField body="prompt" type="string" required>
  图像生成的文本描述，支持中英文
</ParamField>

<ParamField body="aspect_ratio" type="string" default="1:1">
  图像宽高比，可选值： - `1:1` - 正方形（默认） - `16:9` - 横向宽屏 - `9:16` -
  纵向竖屏 - `4:3` - 标准横向 - `3:4` - 标准纵向
</ParamField>

<ParamField body="n" type="integer" default="1">
  生成图像数量，范围 1-4
</ParamField>

## 请求示例

<CodeGroup>

```bash cURL
curl -X POST "https://your-api-domain.com/v1/images/generations" \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "z-image-turbo",
    "prompt": "A futuristic cityscape at sunset, neon lights reflecting on wet streets",
    "aspect_ratio": "16:9",
    "n": 1
  }'
```

```python Python
import requests

response = requests.post(
    "https://your-api-domain.com/v1/images/generations",
    headers={
        "Authorization": "Bearer YOUR_API_KEY",
        "Content-Type": "application/json"
    },
    json={
        "model": "z-image-turbo",
        "prompt": "A futuristic cityscape at sunset, neon lights reflecting on wet streets",
        "aspect_ratio": "16:9",
        "n": 1
    }
)

result = response.json()
print(result)
```

```javascript JavaScript
const response = await fetch(
  "https://your-api-domain.com/v1/images/generations",
  {
    method: "POST",
    headers: {
      Authorization: "Bearer YOUR_API_KEY",
      "Content-Type": "application/json",
    },
    body: JSON.stringify({
      model: "z-image-turbo",
      prompt:
        "A futuristic cityscape at sunset, neon lights reflecting on wet streets",
      aspect_ratio: "16:9",
      n: 1,
    }),
  }
);

const result = await response.json();
console.log(result);
```

</CodeGroup>

## 响应示例

```json
{
  "code": 200,
  "msg": "success",
  "data": {
    "taskId": "task_z-image-turbo_1765174270120"
  }
}
```

## 与 Z-Image 对比

| 特性     | Z-Image    | Z-Image Turbo      |
| -------- | ---------- | ------------------ |
| 生成速度 | 标准       | 极快（亚秒级）     |
| 图像质量 | 高         | 高                 |
| 采样步数 | 标准       | 8 NFE              |
| 适用场景 | 高质量创作 | 实时预览、快速迭代 |

## 最佳实践

<Tip>
  **适用场景**： - 实时创意迭代和预览 - 产品原型快速生成 -
  需要即时反馈的交互式应用
</Tip>

<Note>
  Turbo
  版本在保持高质量的同时大幅提升速度，适合需要快速出图的场景。对于最终成品，可以考虑使用标准版或
  HD 版。
</Note>
